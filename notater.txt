int x = WIDTH / 2;
int y = 0;
for(int i = 0; i < WIDTH; i++)
{
  for(int j = 0; j < HEIGHT; i++)
  {
    /*
	*  Add figure to board in the middle position
	*  Make sure the figure does not extend past the board
	*
	*/
	board[y][j] = 
    if(board[i][j] == 1)
	{
	  drawZero();
	}
	else
	{
	  drawDot();
	}
  }
}